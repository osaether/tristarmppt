<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">

    <item android:id="@+id/menu_about"
        android:icon="@mipmap/ic_action_info"
        android:showAsAction="ifRoom"
        android:title="@string/menu_string_about"/>

    <item android:id="@+id/menu_refresh"
        android:icon="@mipmap/ic_action_refresh"
        android:showAsAction="ifRoom"
        android:title="@string/menu_string_refresh"/>

    <item android:id="@+id/menu_show_settings"
        android:icon="@mipmap/ic_action_settings"
        android:showAsAction="ifRoom"
        android:title="@string/menu_string_settings" />

</menu>
